# Contributing

- Use src layout.
- Write tests for new features.
- Run linters: flake8, black, isort, mypy.
- Keep public APIs documented in docstrings.

## Setup
```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .[dev]
```
